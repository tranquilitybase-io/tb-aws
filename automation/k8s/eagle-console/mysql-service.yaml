apiVersion: v1
kind: Service
metadata:
  name: mysql57
annotations:
    service.beta.kubernetes.io/aws-load-balancer-internal: "0.0.0.0/0"
spec:
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    app: mysql57
  clusterIP: None